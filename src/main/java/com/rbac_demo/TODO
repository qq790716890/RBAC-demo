1. 部门删除后，对应部门的员工怎么处理   还有对应的员工就不允许删除
2. 职位删除后，对应职位的员工怎么处理    还有对应的员工就不允许删除
3. 使用AOP + 注解 来进行权限同一处理。
4. JWT单点登录   cancel




5. 后端安全相关
    5.1 防止A和B同时更改     ---》 数据库加乐观锁。这里直接用update_time 当作乐观锁字段  done
    5.2 防止用户提交不该更改的字段    ---》 1. 对一些敏感的字段，进行权限校验   done
                                 ---》 2. 只提取需要的字段，进行更新数据库 done
    5.3 对输入进行转义         no need
    5.4 对sql字段，设置白名单。       no need
    5.5 CSRF    给所有页面都给一个token，调用别的接口时需要带上该 token   not sure


* 密码加密          done
* token 登陆